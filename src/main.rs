use a3::block::Block;
mod queue;

fn main() {
    //Nothing is required here, but it may be useful for testing.
    // let mut b3 = Block::initial(16);
    // println!("{}", b3.is_valid_for_proof(0));
    // println!("{}", b3.is_valid_for_proof(56231));
    // b3.set_proof(56231);
    // println!("{}", b3.is_valid());

    // let mut b0 = Block::initial(19);
    // b0.set_proof(87745);
    // let mut b1 = Block::next(&b0, String::from("hash example 1234"));
    // b1.set_proof(1407891);
    // println!("{}", b1.is_valid());
    // b1.set_proof(346082);
    // println!("{}", b1.is_valid());

    // let mut b0 = Block::initial(16);
    // b0.set_proof(56231);
    // println!("{}", b0.hash_string_for_proof(56231));
    // println!("{}", b0.hash());
    // let mut b1 = Block::next(&b0, String::from("message"));
    // b1.set_proof(2159);
    // println!("{}", b1.hash_string_for_proof(2159));
    // println!("{}", b1.hash());

}

